interface:
  display_name: "A-Share Data"
  short_description: "Query complete sourced A-share market and company data"
  default_prompt: "Use $a-stock-data to fetch the smallest relevant A-share dataset and explain it with provider and timestamp."

policy:
  allow_implicit_invocation: false
